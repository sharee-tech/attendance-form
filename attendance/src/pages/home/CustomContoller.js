import { Controller } from "react-hook-form";

export default function CustomController(
  name,
  control,
  options,
  value,
  onChange,
  errors,
  formatSavedDates,
  formatDatesUser,
  field,
  renderComponent
) {
  return (
    <Controller
    //   name={name}
    //   control={control}
    //   rules={{ required: true }}
    //   render={}
    />
  );
}
